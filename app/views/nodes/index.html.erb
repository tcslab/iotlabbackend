<h1>Listing nodes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>latitude</th>
      <th>longitude</th>
      <th>provider_id</th>
      <th>Phone flag</th>
      <th>Urn</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @nodes.each do |node| %>
      <tr>
        <td><%= node.name %></td>
        <td><%= node.latitude %></td>
        <td><%= node.longitude %></td>
        <td><%= node.provider_id %></td>
        <td><%= node.phone_flag %></td>
        <td><%= node.urn %></td>
        <td><%= link_to 'Show', node %></td>
        <td><%= link_to 'Edit', edit_node_path(node) %></td>
        <td><%= link_to 'Destroy', node, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Node', new_node_path %>
