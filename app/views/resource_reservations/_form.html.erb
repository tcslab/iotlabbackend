<%= form_for(@resource_reservation) do |f| %>
  <% if @resource_reservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_reservation.errors.count, "error") %> prohibited this resource_reservation from being saved:</h2>

      <ul>
      <% @resource_reservation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reservation_id %><br>
    <%= f.text_field :reservation_id %>
  </div>
  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.text_field :resource_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
